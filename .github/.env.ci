PYTHON_VERSION=3.14.4
UV_VERSION=0.11.7
# Same variable package.py reads (defaulting to this value), so the fixture
# bundle the starling windows tests build matches what packaging produces.
PYINSTALLER_VERSION=6.21.0
