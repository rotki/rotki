codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true

flags:
  backend:
    paths:
      - rotkehlchen
  frontend_unit:
    paths:
      - electron-app
  frontend_integration:
    carryforward: true
    paths:
      - electron-app
