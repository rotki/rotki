/** Minimum and default width of the pinned sidebar in pixels. */
export const PINNED_DEFAULT_WIDTH = 516;

/** Absolute maximum width of the pinned sidebar in pixels. */
export const PINNED_MAX_WIDTH = 1000;

/** Maximum width of the pinned sidebar as a fraction of the viewport width. */
export const PINNED_MAX_VIEWPORT_RATIO = 0.75;
