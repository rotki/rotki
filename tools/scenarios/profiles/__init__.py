"""Profile modules. Each exposes ``build(builder: ProfileBuilder) -> dict | None``
returning optional extra expected values for expected.json. The name→builder
mapping lives in :mod:`tools.scenarios.profiles.registry`."""
